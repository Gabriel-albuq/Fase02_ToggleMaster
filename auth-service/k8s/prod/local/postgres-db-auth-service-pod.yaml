# postgres-db-auth-service
apiVersion: v1
kind: Pod
metadata:
  name: postgres-db-auth-service
  namespace: auth-service
  labels:
    app: auth-service
    instance: postgres-db-auth-service
spec:
  containers:
    - name: postgres-db-auth-service
      image: postgres:18.4-alpine3.24
      resources:
        requests:
          cpu: "100m"
          memory: "256Mi"
        limits:
          cpu: "500m"
          memory: "512Mi"
      ports:
        - containerPort: 5432
      env:
        # Opcao de mudar o name para opaque-auth-service ou usar configmap-auth-service
        - name: POSTGRES_USER
          valueFrom:
            configMapKeyRef:
              name: configmap-auth-service
              key: DB_USERNAME
        - name: POSTGRES_PASSWORD
          valueFrom:
            secretKeyRef:
              name: opaque-auth-service
              key: DB_PASSWORD
        - name: POSTGRES_DB
          valueFrom:
            configMapKeyRef:
              name: configmap-auth-service
              key: DB_DATABASE
      volumeMounts:
        - name: pgdata-auth-service
          mountPath: /var/lib/postgresql
  volumes:
    - name: pgdata-auth-service
      persistentVolumeClaim:
        claimName: pvc-pgdata-auth-service
