apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-pgdata-targeting-service
  namespace: targeting-service
  labels:
    app: pvc-pgdata-targeting-service
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local
  resources:
    requests:
      storage: 50Mi
